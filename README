This package can be used to install orcv, chunknet, largescaleobjects, and largescaler.
Install these packages, and ensure tmux and passwordless localhost ssh.
Then, to get a quick demonstration of LASSO using largescaler, run the following in the shell:

#-----------------------------------------------------------------------------------------------------#
cd `Rscript -e 'cat(system.file(package="largescalemodels"))'`/dev-tests && ./interactive-test.sh lasso
#-----------------------------------------------------------------------------------------------------#

glm and lm be demonstrated in a similar manner through substitution into the above command
